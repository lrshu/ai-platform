# mcp-claude-gpt51 Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-25

## Active Technologies

- Python 3.12 (uv-managed virtual environment) + `fastapi-mcp` for MCP server scaffolding, `pydantic` for validation, `pypinyin` (or equivalent) for transliteration, `python-dotenv` for configuration loading, `secrets`/`uvicorn` runtime utilities (001-provision-mcp-accounts)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.12 (uv-managed virtual environment): Follow standard conventions

## Recent Changes

- 001-provision-mcp-accounts: Added Python 3.12 (uv-managed virtual environment) + `fastapi-mcp` for MCP server scaffolding, `pydantic` for validation, `pypinyin` (or equivalent) for transliteration, `python-dotenv` for configuration loading, `secrets`/`uvicorn` runtime utilities

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
